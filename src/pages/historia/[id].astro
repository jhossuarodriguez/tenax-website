---
import Layout from "../../layouts/Layout.astro";

const { id } = Astro.params;
if (!id) return Astro.redirect("/404");

export const prerender = false;
---

<Layout>
    <section
        class="w-full min-h-screen flex flex-col md:flex-row justify-center items-center bg-[url('/about-bg.webp')] bg-cover bg-center bg-no-repeat mx-auto"
    >
        <div class="flex flex-col justify-center items-center">
            <header
                class="relative text-3xl md:text-4xl font-semibold text-white/75"
            >
                Nuestra Historia
                <span
                    class="absolute top-12 left-0 bottom-0 w-full h-1 bg-[#F46738] scale-x-100 origin-left"
                ></span>
            </header>

            <div
                class="w-[400px] md:w-[800px] h-full flex flex-col justify-center items-start mt-10 mx-10 backdrop-blur-md bg-white/10 border border-white/20 rounded-lg p-10 shadow-lg"
            >
                <p
                    class="text-white/75 mt-5 leading-relaxed text-sm md:text-lg"
                >
                    Somos una constructora dominicana dedicada a la
                    administración, construcción, diseño, y supervisión de obras
                    civiles, además de realizar estudios de viabilidad
                    socio-económico y ambiental para respaldar cada proyecto.
                    <br />
                    <br />Fundada en el año 2012. Nuestra experiencia nos
                    permite el manejo de proyectos altamente detallados,
                    garantizando una óptima supervisión, para brindar excelentes
                    resultados.
                </p>
            </div>
        </div>
        <img
            data-id={id}
            src={`/logo-tenax.webp`}
            alt="Logo de Tenax Construction SRL"
            class="w-[250px] md:w-[800px] object-cover"
            loading="lazy"
        />
    </section>
</Layout>
